/**
 * Generated by packages/api-doc-viewer/bin/generate-json-schema-type-changes-samples.mjs
 * Edit together with src/stories/json-schema-diffs-suite/one-of-array-variant.stories.tsx
 */
import { StoryPage } from "../service/story-page";
import { ViewComponent } from "../service/view-component";
import { storyPage } from "../service/storybook-service";

const META_ID = "json-schema-diffs-suite-oneof-array-variant-samples";

async function waitForJsonSchemaDiffViewer() {
  await page.waitForSelector('[data-name="JsonNode"]', { visible: true });
  await page.waitForFunction(() => document.readyState === "complete");
  await page.evaluate(() => new Promise<void>((resolve) =>
    requestAnimationFrame(() => requestAnimationFrame(() => resolve())),
  ));
}

describe("JSON Schema Diffs Suite/OneOf Array Variant Samples", () => {
  let story: StoryPage;
  let component: ViewComponent;

  beforeEach(async () => {
    await jestPuppeteer.resetPage();
  });

  it("001-array-variant-add-one-indexed-item-string", async () => {
    story = await storyPage(page, `json-schema-diffs-suite-oneof-array-variant-samples--case-001-array-variant-add-one-indexed-item-string`);
    await waitForJsonSchemaDiffViewer();
    component = await story.viewComponent();
    expect(await component.captureScreenshot()).toMatchImageSnapshot();
  });

  it("002-array-variant-remove-one-indexed-item-string", async () => {
    story = await storyPage(page, `json-schema-diffs-suite-oneof-array-variant-samples--case-002-array-variant-remove-one-indexed-item-string`);
    await waitForJsonSchemaDiffViewer();
    component = await story.viewComponent();
    expect(await component.captureScreenshot()).toMatchImageSnapshot();
  });

  it("003-array-variant-add-two-indexed-items-string", async () => {
    story = await storyPage(page, `json-schema-diffs-suite-oneof-array-variant-samples--case-003-array-variant-add-two-indexed-items-string`);
    await waitForJsonSchemaDiffViewer();
    component = await story.viewComponent();
    expect(await component.captureScreenshot()).toMatchImageSnapshot();
  });

  it("004-array-variant-remove-two-indexed-items-string", async () => {
    story = await storyPage(page, `json-schema-diffs-suite-oneof-array-variant-samples--case-004-array-variant-remove-two-indexed-items-string`);
    await waitForJsonSchemaDiffViewer();
    component = await story.viewComponent();
    expect(await component.captureScreenshot()).toMatchImageSnapshot();
  });

  it("005-array-variant-added-string", async () => {
    story = await storyPage(page, `json-schema-diffs-suite-oneof-array-variant-samples--case-005-array-variant-added-string`);
    await waitForJsonSchemaDiffViewer();
    component = await story.viewComponent();
    expect(await component.captureScreenshot()).toMatchImageSnapshot();
  });

  it("006-array-variant-removed-string", async () => {
    story = await storyPage(page, `json-schema-diffs-suite-oneof-array-variant-samples--case-006-array-variant-removed-string`);
    await waitForJsonSchemaDiffViewer();
    component = await story.viewComponent();
    expect(await component.captureScreenshot()).toMatchImageSnapshot();
  });

  it("007-one-of-prop-added-string", async () => {
    story = await storyPage(page, `json-schema-diffs-suite-oneof-array-variant-samples--case-007-one-of-prop-added-string`);
    await waitForJsonSchemaDiffViewer();
    component = await story.viewComponent();
    expect(await component.captureScreenshot()).toMatchImageSnapshot();
  });

  it("008-one-of-prop-removed-string", async () => {
    story = await storyPage(page, `json-schema-diffs-suite-oneof-array-variant-samples--case-008-one-of-prop-removed-string`);
    await waitForJsonSchemaDiffViewer();
    component = await story.viewComponent();
    expect(await component.captureScreenshot()).toMatchImageSnapshot();
  });

  it("009-array-variant-add-one-indexed-item-number", async () => {
    story = await storyPage(page, `json-schema-diffs-suite-oneof-array-variant-samples--case-009-array-variant-add-one-indexed-item-number`);
    await waitForJsonSchemaDiffViewer();
    component = await story.viewComponent();
    expect(await component.captureScreenshot()).toMatchImageSnapshot();
  });

  it("010-array-variant-remove-one-indexed-item-number", async () => {
    story = await storyPage(page, `json-schema-diffs-suite-oneof-array-variant-samples--case-010-array-variant-remove-one-indexed-item-number`);
    await waitForJsonSchemaDiffViewer();
    component = await story.viewComponent();
    expect(await component.captureScreenshot()).toMatchImageSnapshot();
  });

  it("011-array-variant-add-two-indexed-items-number", async () => {
    story = await storyPage(page, `json-schema-diffs-suite-oneof-array-variant-samples--case-011-array-variant-add-two-indexed-items-number`);
    await waitForJsonSchemaDiffViewer();
    component = await story.viewComponent();
    expect(await component.captureScreenshot()).toMatchImageSnapshot();
  });

  it("012-array-variant-remove-two-indexed-items-number", async () => {
    story = await storyPage(page, `json-schema-diffs-suite-oneof-array-variant-samples--case-012-array-variant-remove-two-indexed-items-number`);
    await waitForJsonSchemaDiffViewer();
    component = await story.viewComponent();
    expect(await component.captureScreenshot()).toMatchImageSnapshot();
  });

  it("013-array-variant-added-number", async () => {
    story = await storyPage(page, `json-schema-diffs-suite-oneof-array-variant-samples--case-013-array-variant-added-number`);
    await waitForJsonSchemaDiffViewer();
    component = await story.viewComponent();
    expect(await component.captureScreenshot()).toMatchImageSnapshot();
  });

  it("014-array-variant-removed-number", async () => {
    story = await storyPage(page, `json-schema-diffs-suite-oneof-array-variant-samples--case-014-array-variant-removed-number`);
    await waitForJsonSchemaDiffViewer();
    component = await story.viewComponent();
    expect(await component.captureScreenshot()).toMatchImageSnapshot();
  });

  it("015-one-of-prop-added-number", async () => {
    story = await storyPage(page, `json-schema-diffs-suite-oneof-array-variant-samples--case-015-one-of-prop-added-number`);
    await waitForJsonSchemaDiffViewer();
    component = await story.viewComponent();
    expect(await component.captureScreenshot()).toMatchImageSnapshot();
  });

  it("016-one-of-prop-removed-number", async () => {
    story = await storyPage(page, `json-schema-diffs-suite-oneof-array-variant-samples--case-016-one-of-prop-removed-number`);
    await waitForJsonSchemaDiffViewer();
    component = await story.viewComponent();
    expect(await component.captureScreenshot()).toMatchImageSnapshot();
  });

  it("017-array-variant-add-one-indexed-item-integer", async () => {
    story = await storyPage(page, `json-schema-diffs-suite-oneof-array-variant-samples--case-017-array-variant-add-one-indexed-item-integer`);
    await waitForJsonSchemaDiffViewer();
    component = await story.viewComponent();
    expect(await component.captureScreenshot()).toMatchImageSnapshot();
  });

  it("018-array-variant-remove-one-indexed-item-integer", async () => {
    story = await storyPage(page, `json-schema-diffs-suite-oneof-array-variant-samples--case-018-array-variant-remove-one-indexed-item-integer`);
    await waitForJsonSchemaDiffViewer();
    component = await story.viewComponent();
    expect(await component.captureScreenshot()).toMatchImageSnapshot();
  });

  it("019-array-variant-add-two-indexed-items-integer", async () => {
    story = await storyPage(page, `json-schema-diffs-suite-oneof-array-variant-samples--case-019-array-variant-add-two-indexed-items-integer`);
    await waitForJsonSchemaDiffViewer();
    component = await story.viewComponent();
    expect(await component.captureScreenshot()).toMatchImageSnapshot();
  });

  it("020-array-variant-remove-two-indexed-items-integer", async () => {
    story = await storyPage(page, `json-schema-diffs-suite-oneof-array-variant-samples--case-020-array-variant-remove-two-indexed-items-integer`);
    await waitForJsonSchemaDiffViewer();
    component = await story.viewComponent();
    expect(await component.captureScreenshot()).toMatchImageSnapshot();
  });

  it("021-array-variant-added-integer", async () => {
    story = await storyPage(page, `json-schema-diffs-suite-oneof-array-variant-samples--case-021-array-variant-added-integer`);
    await waitForJsonSchemaDiffViewer();
    component = await story.viewComponent();
    expect(await component.captureScreenshot()).toMatchImageSnapshot();
  });

  it("022-array-variant-removed-integer", async () => {
    story = await storyPage(page, `json-schema-diffs-suite-oneof-array-variant-samples--case-022-array-variant-removed-integer`);
    await waitForJsonSchemaDiffViewer();
    component = await story.viewComponent();
    expect(await component.captureScreenshot()).toMatchImageSnapshot();
  });

  it("023-one-of-prop-added-integer", async () => {
    story = await storyPage(page, `json-schema-diffs-suite-oneof-array-variant-samples--case-023-one-of-prop-added-integer`);
    await waitForJsonSchemaDiffViewer();
    component = await story.viewComponent();
    expect(await component.captureScreenshot()).toMatchImageSnapshot();
  });

  it("024-one-of-prop-removed-integer", async () => {
    story = await storyPage(page, `json-schema-diffs-suite-oneof-array-variant-samples--case-024-one-of-prop-removed-integer`);
    await waitForJsonSchemaDiffViewer();
    component = await story.viewComponent();
    expect(await component.captureScreenshot()).toMatchImageSnapshot();
  });

  it("025-array-variant-add-one-indexed-item-boolean", async () => {
    story = await storyPage(page, `json-schema-diffs-suite-oneof-array-variant-samples--case-025-array-variant-add-one-indexed-item-boolean`);
    await waitForJsonSchemaDiffViewer();
    component = await story.viewComponent();
    expect(await component.captureScreenshot()).toMatchImageSnapshot();
  });

  it("026-array-variant-remove-one-indexed-item-boolean", async () => {
    story = await storyPage(page, `json-schema-diffs-suite-oneof-array-variant-samples--case-026-array-variant-remove-one-indexed-item-boolean`);
    await waitForJsonSchemaDiffViewer();
    component = await story.viewComponent();
    expect(await component.captureScreenshot()).toMatchImageSnapshot();
  });

  it("027-array-variant-add-two-indexed-items-boolean", async () => {
    story = await storyPage(page, `json-schema-diffs-suite-oneof-array-variant-samples--case-027-array-variant-add-two-indexed-items-boolean`);
    await waitForJsonSchemaDiffViewer();
    component = await story.viewComponent();
    expect(await component.captureScreenshot()).toMatchImageSnapshot();
  });

  it("028-array-variant-remove-two-indexed-items-boolean", async () => {
    story = await storyPage(page, `json-schema-diffs-suite-oneof-array-variant-samples--case-028-array-variant-remove-two-indexed-items-boolean`);
    await waitForJsonSchemaDiffViewer();
    component = await story.viewComponent();
    expect(await component.captureScreenshot()).toMatchImageSnapshot();
  });

  it("029-array-variant-added-boolean", async () => {
    story = await storyPage(page, `json-schema-diffs-suite-oneof-array-variant-samples--case-029-array-variant-added-boolean`);
    await waitForJsonSchemaDiffViewer();
    component = await story.viewComponent();
    expect(await component.captureScreenshot()).toMatchImageSnapshot();
  });

  it("030-array-variant-removed-boolean", async () => {
    story = await storyPage(page, `json-schema-diffs-suite-oneof-array-variant-samples--case-030-array-variant-removed-boolean`);
    await waitForJsonSchemaDiffViewer();
    component = await story.viewComponent();
    expect(await component.captureScreenshot()).toMatchImageSnapshot();
  });

  it("031-one-of-prop-added-boolean", async () => {
    story = await storyPage(page, `json-schema-diffs-suite-oneof-array-variant-samples--case-031-one-of-prop-added-boolean`);
    await waitForJsonSchemaDiffViewer();
    component = await story.viewComponent();
    expect(await component.captureScreenshot()).toMatchImageSnapshot();
  });

  it("032-one-of-prop-removed-boolean", async () => {
    story = await storyPage(page, `json-schema-diffs-suite-oneof-array-variant-samples--case-032-one-of-prop-removed-boolean`);
    await waitForJsonSchemaDiffViewer();
    component = await story.viewComponent();
    expect(await component.captureScreenshot()).toMatchImageSnapshot();
  });

  it("033-array-variant-add-one-indexed-item-array", async () => {
    story = await storyPage(page, `json-schema-diffs-suite-oneof-array-variant-samples--case-033-array-variant-add-one-indexed-item-array`);
    await waitForJsonSchemaDiffViewer();
    component = await story.viewComponent();
    expect(await component.captureScreenshot()).toMatchImageSnapshot();
  });

  it("034-array-variant-remove-one-indexed-item-array", async () => {
    story = await storyPage(page, `json-schema-diffs-suite-oneof-array-variant-samples--case-034-array-variant-remove-one-indexed-item-array`);
    await waitForJsonSchemaDiffViewer();
    component = await story.viewComponent();
    expect(await component.captureScreenshot()).toMatchImageSnapshot();
  });

  it("035-array-variant-add-two-indexed-items-array", async () => {
    story = await storyPage(page, `json-schema-diffs-suite-oneof-array-variant-samples--case-035-array-variant-add-two-indexed-items-array`);
    await waitForJsonSchemaDiffViewer();
    component = await story.viewComponent();
    expect(await component.captureScreenshot()).toMatchImageSnapshot();
  });

  it("036-array-variant-remove-two-indexed-items-array", async () => {
    story = await storyPage(page, `json-schema-diffs-suite-oneof-array-variant-samples--case-036-array-variant-remove-two-indexed-items-array`);
    await waitForJsonSchemaDiffViewer();
    component = await story.viewComponent();
    expect(await component.captureScreenshot()).toMatchImageSnapshot();
  });

  it("037-array-variant-added-array", async () => {
    story = await storyPage(page, `json-schema-diffs-suite-oneof-array-variant-samples--case-037-array-variant-added-array`);
    await waitForJsonSchemaDiffViewer();
    component = await story.viewComponent();
    expect(await component.captureScreenshot()).toMatchImageSnapshot();
  });

  it("038-array-variant-removed-array", async () => {
    story = await storyPage(page, `json-schema-diffs-suite-oneof-array-variant-samples--case-038-array-variant-removed-array`);
    await waitForJsonSchemaDiffViewer();
    component = await story.viewComponent();
    expect(await component.captureScreenshot()).toMatchImageSnapshot();
  });

  it("039-one-of-prop-added-array", async () => {
    story = await storyPage(page, `json-schema-diffs-suite-oneof-array-variant-samples--case-039-one-of-prop-added-array`);
    await waitForJsonSchemaDiffViewer();
    component = await story.viewComponent();
    expect(await component.captureScreenshot()).toMatchImageSnapshot();
  });

  it("040-one-of-prop-removed-array", async () => {
    story = await storyPage(page, `json-schema-diffs-suite-oneof-array-variant-samples--case-040-one-of-prop-removed-array`);
    await waitForJsonSchemaDiffViewer();
    component = await story.viewComponent();
    expect(await component.captureScreenshot()).toMatchImageSnapshot();
  });

  it("041-array-variant-add-one-indexed-item-object", async () => {
    story = await storyPage(page, `json-schema-diffs-suite-oneof-array-variant-samples--case-041-array-variant-add-one-indexed-item-object`);
    await waitForJsonSchemaDiffViewer();
    component = await story.viewComponent();
    expect(await component.captureScreenshot()).toMatchImageSnapshot();
  });

  it("042-array-variant-remove-one-indexed-item-object", async () => {
    story = await storyPage(page, `json-schema-diffs-suite-oneof-array-variant-samples--case-042-array-variant-remove-one-indexed-item-object`);
    await waitForJsonSchemaDiffViewer();
    component = await story.viewComponent();
    expect(await component.captureScreenshot()).toMatchImageSnapshot();
  });

  it("043-array-variant-add-two-indexed-items-object", async () => {
    story = await storyPage(page, `json-schema-diffs-suite-oneof-array-variant-samples--case-043-array-variant-add-two-indexed-items-object`);
    await waitForJsonSchemaDiffViewer();
    component = await story.viewComponent();
    expect(await component.captureScreenshot()).toMatchImageSnapshot();
  });

  it("044-array-variant-remove-two-indexed-items-object", async () => {
    story = await storyPage(page, `json-schema-diffs-suite-oneof-array-variant-samples--case-044-array-variant-remove-two-indexed-items-object`);
    await waitForJsonSchemaDiffViewer();
    component = await story.viewComponent();
    expect(await component.captureScreenshot()).toMatchImageSnapshot();
  });

  it("045-array-variant-added-object", async () => {
    story = await storyPage(page, `json-schema-diffs-suite-oneof-array-variant-samples--case-045-array-variant-added-object`);
    await waitForJsonSchemaDiffViewer();
    component = await story.viewComponent();
    expect(await component.captureScreenshot()).toMatchImageSnapshot();
  });

  it("046-array-variant-removed-object", async () => {
    story = await storyPage(page, `json-schema-diffs-suite-oneof-array-variant-samples--case-046-array-variant-removed-object`);
    await waitForJsonSchemaDiffViewer();
    component = await story.viewComponent();
    expect(await component.captureScreenshot()).toMatchImageSnapshot();
  });

  it("047-one-of-prop-added-object", async () => {
    story = await storyPage(page, `json-schema-diffs-suite-oneof-array-variant-samples--case-047-one-of-prop-added-object`);
    await waitForJsonSchemaDiffViewer();
    component = await story.viewComponent();
    expect(await component.captureScreenshot()).toMatchImageSnapshot();
  });

  it("048-one-of-prop-removed-object", async () => {
    story = await storyPage(page, `json-schema-diffs-suite-oneof-array-variant-samples--case-048-one-of-prop-removed-object`);
    await waitForJsonSchemaDiffViewer();
    component = await story.viewComponent();
    expect(await component.captureScreenshot()).toMatchImageSnapshot();
  });
});
