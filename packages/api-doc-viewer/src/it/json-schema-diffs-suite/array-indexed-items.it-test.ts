/**
 * Generated by packages/api-doc-viewer/bin/generate-json-schema-type-changes-samples.mjs
 * Edit together with src/stories/json-schema-diffs-suite/array-indexed-items.stories.tsx
 */
import { StoryPage } from "../service/story-page";
import { ViewComponent } from "../service/view-component";
import { storyPage } from "../service/storybook-service";

const META_ID = "json-schema-diffs-suite-array-indexed-items-samples";

async function waitForJsonSchemaDiffViewer() {
  await page.waitForSelector('[data-testid="json-schema-next-diffs-viewer"]', { visible: true });
  await page.waitForFunction(() => {
    for (const selector of ['[data-name="JsonNode"]', '[data-testid="json-schema-combiner-node-viewer"]']) {
      const element = document.querySelector(selector);
      if (!element) {
        continue;
      }
      const rect = element.getBoundingClientRect();
      if (rect.width > 0 && rect.height > 0) {
        return true;
      }
    }
    return false;
  });
  await page.waitForFunction(() => document.readyState === "complete");
  await page.evaluate(() => new Promise<void>((resolve) =>
    requestAnimationFrame(() => requestAnimationFrame(() => resolve())),
  ));
}

describe("JSON Schema Diffs Suite/Array Indexed Items Samples", () => {
  let story: StoryPage;
  let component: ViewComponent;

  beforeEach(async () => {
    await jestPuppeteer.resetPage();
  });

  it("001-add-one-indexed-item-string", async () => {
    story = await storyPage(page, `json-schema-diffs-suite-array-indexed-items-samples--case-001-add-one-indexed-item-string`);
    await waitForJsonSchemaDiffViewer();
    component = await story.viewComponent();
    expect(await component.captureScreenshot()).toMatchImageSnapshot();
  });

  it("002-remove-one-indexed-item-string", async () => {
    story = await storyPage(page, `json-schema-diffs-suite-array-indexed-items-samples--case-002-remove-one-indexed-item-string`);
    await waitForJsonSchemaDiffViewer();
    component = await story.viewComponent();
    expect(await component.captureScreenshot()).toMatchImageSnapshot();
  });

  it("003-add-two-indexed-items-string", async () => {
    story = await storyPage(page, `json-schema-diffs-suite-array-indexed-items-samples--case-003-add-two-indexed-items-string`);
    await waitForJsonSchemaDiffViewer();
    component = await story.viewComponent();
    expect(await component.captureScreenshot()).toMatchImageSnapshot();
  });

  it("004-remove-two-indexed-items-string", async () => {
    story = await storyPage(page, `json-schema-diffs-suite-array-indexed-items-samples--case-004-remove-two-indexed-items-string`);
    await waitForJsonSchemaDiffViewer();
    component = await story.viewComponent();
    expect(await component.captureScreenshot()).toMatchImageSnapshot();
  });

  it("005-add-one-indexed-item-number", async () => {
    story = await storyPage(page, `json-schema-diffs-suite-array-indexed-items-samples--case-005-add-one-indexed-item-number`);
    await waitForJsonSchemaDiffViewer();
    component = await story.viewComponent();
    expect(await component.captureScreenshot()).toMatchImageSnapshot();
  });

  it("006-remove-one-indexed-item-number", async () => {
    story = await storyPage(page, `json-schema-diffs-suite-array-indexed-items-samples--case-006-remove-one-indexed-item-number`);
    await waitForJsonSchemaDiffViewer();
    component = await story.viewComponent();
    expect(await component.captureScreenshot()).toMatchImageSnapshot();
  });

  it("007-add-two-indexed-items-number", async () => {
    story = await storyPage(page, `json-schema-diffs-suite-array-indexed-items-samples--case-007-add-two-indexed-items-number`);
    await waitForJsonSchemaDiffViewer();
    component = await story.viewComponent();
    expect(await component.captureScreenshot()).toMatchImageSnapshot();
  });

  it("008-remove-two-indexed-items-number", async () => {
    story = await storyPage(page, `json-schema-diffs-suite-array-indexed-items-samples--case-008-remove-two-indexed-items-number`);
    await waitForJsonSchemaDiffViewer();
    component = await story.viewComponent();
    expect(await component.captureScreenshot()).toMatchImageSnapshot();
  });

  it("009-add-one-indexed-item-integer", async () => {
    story = await storyPage(page, `json-schema-diffs-suite-array-indexed-items-samples--case-009-add-one-indexed-item-integer`);
    await waitForJsonSchemaDiffViewer();
    component = await story.viewComponent();
    expect(await component.captureScreenshot()).toMatchImageSnapshot();
  });

  it("010-remove-one-indexed-item-integer", async () => {
    story = await storyPage(page, `json-schema-diffs-suite-array-indexed-items-samples--case-010-remove-one-indexed-item-integer`);
    await waitForJsonSchemaDiffViewer();
    component = await story.viewComponent();
    expect(await component.captureScreenshot()).toMatchImageSnapshot();
  });

  it("011-add-two-indexed-items-integer", async () => {
    story = await storyPage(page, `json-schema-diffs-suite-array-indexed-items-samples--case-011-add-two-indexed-items-integer`);
    await waitForJsonSchemaDiffViewer();
    component = await story.viewComponent();
    expect(await component.captureScreenshot()).toMatchImageSnapshot();
  });

  it("012-remove-two-indexed-items-integer", async () => {
    story = await storyPage(page, `json-schema-diffs-suite-array-indexed-items-samples--case-012-remove-two-indexed-items-integer`);
    await waitForJsonSchemaDiffViewer();
    component = await story.viewComponent();
    expect(await component.captureScreenshot()).toMatchImageSnapshot();
  });

  it("013-add-one-indexed-item-boolean", async () => {
    story = await storyPage(page, `json-schema-diffs-suite-array-indexed-items-samples--case-013-add-one-indexed-item-boolean`);
    await waitForJsonSchemaDiffViewer();
    component = await story.viewComponent();
    expect(await component.captureScreenshot()).toMatchImageSnapshot();
  });

  it("014-remove-one-indexed-item-boolean", async () => {
    story = await storyPage(page, `json-schema-diffs-suite-array-indexed-items-samples--case-014-remove-one-indexed-item-boolean`);
    await waitForJsonSchemaDiffViewer();
    component = await story.viewComponent();
    expect(await component.captureScreenshot()).toMatchImageSnapshot();
  });

  it("015-add-two-indexed-items-boolean", async () => {
    story = await storyPage(page, `json-schema-diffs-suite-array-indexed-items-samples--case-015-add-two-indexed-items-boolean`);
    await waitForJsonSchemaDiffViewer();
    component = await story.viewComponent();
    expect(await component.captureScreenshot()).toMatchImageSnapshot();
  });

  it("016-remove-two-indexed-items-boolean", async () => {
    story = await storyPage(page, `json-schema-diffs-suite-array-indexed-items-samples--case-016-remove-two-indexed-items-boolean`);
    await waitForJsonSchemaDiffViewer();
    component = await story.viewComponent();
    expect(await component.captureScreenshot()).toMatchImageSnapshot();
  });

  it("017-add-one-indexed-item-array", async () => {
    story = await storyPage(page, `json-schema-diffs-suite-array-indexed-items-samples--case-017-add-one-indexed-item-array`);
    await waitForJsonSchemaDiffViewer();
    component = await story.viewComponent();
    expect(await component.captureScreenshot()).toMatchImageSnapshot();
  });

  it("018-remove-one-indexed-item-array", async () => {
    story = await storyPage(page, `json-schema-diffs-suite-array-indexed-items-samples--case-018-remove-one-indexed-item-array`);
    await waitForJsonSchemaDiffViewer();
    component = await story.viewComponent();
    expect(await component.captureScreenshot()).toMatchImageSnapshot();
  });

  it("019-add-two-indexed-items-array", async () => {
    story = await storyPage(page, `json-schema-diffs-suite-array-indexed-items-samples--case-019-add-two-indexed-items-array`);
    await waitForJsonSchemaDiffViewer();
    component = await story.viewComponent();
    expect(await component.captureScreenshot()).toMatchImageSnapshot();
  });

  it("020-remove-two-indexed-items-array", async () => {
    story = await storyPage(page, `json-schema-diffs-suite-array-indexed-items-samples--case-020-remove-two-indexed-items-array`);
    await waitForJsonSchemaDiffViewer();
    component = await story.viewComponent();
    expect(await component.captureScreenshot()).toMatchImageSnapshot();
  });

  it("021-add-one-indexed-item-object", async () => {
    story = await storyPage(page, `json-schema-diffs-suite-array-indexed-items-samples--case-021-add-one-indexed-item-object`);
    await waitForJsonSchemaDiffViewer();
    component = await story.viewComponent();
    expect(await component.captureScreenshot()).toMatchImageSnapshot();
  });

  it("022-remove-one-indexed-item-object", async () => {
    story = await storyPage(page, `json-schema-diffs-suite-array-indexed-items-samples--case-022-remove-one-indexed-item-object`);
    await waitForJsonSchemaDiffViewer();
    component = await story.viewComponent();
    expect(await component.captureScreenshot()).toMatchImageSnapshot();
  });

  it("023-add-two-indexed-items-object", async () => {
    story = await storyPage(page, `json-schema-diffs-suite-array-indexed-items-samples--case-023-add-two-indexed-items-object`);
    await waitForJsonSchemaDiffViewer();
    component = await story.viewComponent();
    expect(await component.captureScreenshot()).toMatchImageSnapshot();
  });

  it("024-remove-two-indexed-items-object", async () => {
    story = await storyPage(page, `json-schema-diffs-suite-array-indexed-items-samples--case-024-remove-two-indexed-items-object`);
    await waitForJsonSchemaDiffViewer();
    component = await story.viewComponent();
    expect(await component.captureScreenshot()).toMatchImageSnapshot();
  });
});
