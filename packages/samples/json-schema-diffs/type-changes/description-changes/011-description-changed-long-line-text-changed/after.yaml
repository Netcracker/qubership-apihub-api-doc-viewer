type: string
description: This description elaborates on the semantics of the field, including validation constraints, default behavior when omitted, backward-compatibility guarantees across API versions, and guidance for client implementations that need to interpret the value correctly under all supported configurations. This description elaborates on the semantics of the field, including validation constraints, default behavior when omitted, backward-compatibility guarantees across API versions, and guidance for client implementations that need to interpret the value correctly under all supported configurations. This description elaborates on the semantics of the field, including validation constraints, default behavior when omitted, backward-compatibility guarantees across API versions, and guidance for client implementations that need to interpret the value correctly under all supported configurations. This description elaborates on the semantics of the field, including validation constraints, default behav

