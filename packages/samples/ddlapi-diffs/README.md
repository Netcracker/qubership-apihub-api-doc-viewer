# DDL API diff fixtures

These fixtures feed screenshot-diff scenarios for DDL table changes.

- Total cases: 147
- File layout: `ddlapi-diffs/<case-id>/before.sql` and `ddlapi-diffs/<case-id>/after.sql`
- Case numbering follows the request order; generated type/parameter/enum sections continue the numbering.
